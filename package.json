{
  "name": "server-to-server-app",
  "version": "1.0.0",
  "description": "This sample app uses the Server to Server OAuth app to generate tokens, used to call the Zoom API.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "Ojus Save and Elisa Levet",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "nodemon": "^2.0.19",
    "request": "^2.88.2"
  }
}
